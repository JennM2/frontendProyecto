import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles((theme) => ({
    subtitle: {
        fontFamily: "Poppins-Semibold",
        fontSize: "1.5vw",
        margin: "2vw 0vw 3vw 7vw",
    },
    tableSchedule: {
        marginLeft: "8.5vw",
    },

}));

export default useStyles;